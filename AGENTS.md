# 仓库维护规则

- 本仓库由单人维护。修改、创建或验证 `skills/` 下的工作流 Skill 时，默认直接在 `main` 分支完成并推送。
- 不要为本仓库的 Skill 修改创建功能分支或 Git worktree；只有用户明确要求隔离开发时才允许创建。
- 提交前只暂存本次修改涉及的明确文件，避免把临时目录、验证产物或无关 worktree 内容加入提交。
- 提交信息及详情使用中文，并带有 `fix:`、`feat:` 等类型前缀。
